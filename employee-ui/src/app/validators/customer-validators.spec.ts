import { CustomerValidators } from './customer-validators';

describe('CustomerValidators', () => {
  it('should create an instance', () => {
    expect(new CustomerValidators()).toBeTruthy();
  });
});
